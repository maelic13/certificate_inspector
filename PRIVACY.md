# Privacy

Certificate Inspector does not collect, transmit, sell, or store personal data outside Firefox.

The extension reads the active tab URL and TLS certificate information exposed by Firefox so it can classify the current site's certificate chain. The latest certificate status is kept in extension memory for display in the toolbar popup.

No analytics, remote logging, tracking pixels, external services, or network calls are used by the extension.

