{
  "name": "certificate-inspector",
  "version": "1.0.0",
  "private": true,
  "description": "Firefox WebExtension that highlights local or system certificate trust.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "lint": "web-ext lint",
    "run": "web-ext run",
    "build": "web-ext build --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^8.9.0"
  }
}
